<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.WAKE_LOCK" />
    <uses-permission android:name="android.permission.READ_MEDIA_IMAGES" />
    <!-- Фоновая музыка с устройства -->
    <uses-permission android:name="android.permission.READ_MEDIA_AUDIO" />
    <!-- Удержание процессора и Wi-Fi, пока играет радио: без этого
         приставка, экономя энергию, роняла поток через пару минут -->
    <uses-permission android:name="android.permission.WAKE_LOCK" />
    <!-- Android 14: пользователь может открыть доступ не ко всей галерее, а
         к выбранным снимкам. Без этого разрешения такой выбор выглядит как
         отказ, и рамка просит доступ при каждом запуске. -->
    <uses-permission android:name="android.permission.READ_MEDIA_VISUAL_USER_SELECTED" />
    <!-- Нужно, чтобы MediaStore отдавал координаты съёмки для подписи города -->
    <uses-permission android:name="android.permission.ACCESS_MEDIA_LOCATION" />
    <uses-permission
        android:name="android.permission.READ_EXTERNAL_STORAGE"
        android:maxSdkVersion="32" />

    <!-- ТВ-приставки не имеют тачскрина и камеры: без required=false
         приложение не появится в Google Play для Android TV. -->
    <uses-feature
        android:name="android.hardware.touchscreen"
        android:required="false" />
    <uses-feature
        android:name="android.software.leanback"
        android:required="false" />

    <!--
      usesCleartextTraffic: незашифрованные подключения нужны только
      радиопотокам — многие станции, особенно российские, вещают по http.
      Всё остальное (погода, Яндекс.Диск, определение города) приложение
      само запрашивает по https.
    -->
    <application
        android:name=".App"
        android:allowBackup="false"
        android:banner="@drawable/tv_banner"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:largeHeap="true"
        android:usesCleartextTraffic="true"
        android:supportsRtl="true"
        android:theme="@style/Theme.FotoFrame">

        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:configChanges="orientation|screenSize|keyboardHidden"
            android:windowSoftInputMode="adjustResize"
            android:launchMode="singleTop"
            android:theme="@style/Theme.FotoFrame">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
            <!-- Точка входа для Android TV -->
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LEANBACK_LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- Заставка: приложение можно выбрать в системных настройках экрана -->
        <service
            android:name=".FrameDreamService"
            android:exported="true"
            android:label="@string/app_name"
            android:permission="android.permission.BIND_DREAM_SERVICE">
            <intent-filter>
                <action android:name="android.service.dreams.DreamService" />
                <category android:name="android.intent.category.DEFAULT" />
            </intent-filter>
            <meta-data
                android:name="android.service.dream"
                android:resource="@xml/dream_info" />
        </service>

    </application>
</manifest>
